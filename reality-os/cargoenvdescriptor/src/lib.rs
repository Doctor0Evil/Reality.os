impl CargoEnvDescriptor {
    pub fn current_for_neurostack() -> Self {
        // ...
        let blakeposture = BlakeCryptoPosture::forbid_all();
        CargoEnvDescriptor {
            host_toolchain,
            safety_neurorights,
            dev_tunnel,
            blake_posture: blakeposture,
        }
    }
}
